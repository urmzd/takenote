struct Message {
    message: String,
    date: String,
    // By default, this should be the computer's name.
    author: String,
}

struct File {
    filename: String,
    dateCreated: String,
    dateLastUpdated: String,
}

fn createMarkdown() {}
